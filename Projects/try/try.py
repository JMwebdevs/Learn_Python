import math

print("\n\tRadius to Circumference\n")

radius = float(input("Enter a radius of a circle: "))
circumference = 2 * math * radius
print(f"The Circumference of a circle is {round(circumference, 2)}")